contexts:
  - name: postalcode
    view: postalcode
    table: postalcode
    key: id
    records: 10
    maxrecords: 100
    paging: true
    repeat-control: confirm-insert confirm-delete
    query:
      - field: f3
        value: 1%
        operator: LIKE
    sort:
      - field: f3
        direction: ASC
options:
  - theme: default
connection:
  db-class: PDO
debug: 2