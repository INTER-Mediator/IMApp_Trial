contexts:
    -
        name: postalcode
        table: postalcode
        view: postalcode
        key: id
        paging: true
        records: 10
        maxrecords: 1000
options: {  }
connection:
    db-class: PDO
debug: false

